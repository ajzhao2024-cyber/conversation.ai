{
  "name": "conversation-ai",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "node --env-file-if-exists=.env.local scripts/dev-server.js",
    "test": "node --test"
  },
  "engines": {
    "node": ">=20"
  }
}
